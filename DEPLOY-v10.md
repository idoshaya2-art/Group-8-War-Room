# מנה 3 · v10.0 — והעלאה לגיטהאב

## קודם: אני לא יכול לדחוף בעצמי

אין בסביבה הזו הרשאות לריפו שלך — לא טוקן, לא `gh`, לא credentials. עשיתי commit מקומי מלא עם הודעה מפורטת, וארזתי אותו כ-patch. **ההעלאה היא הדבקה אחת אצלך.**

### אפשרות א׳ — הכי פשוטה (החלפת קבצים)

הורד את שלושת הקבצים והעתק אותם לריפו:

```bash
cd Group-8-War-Room
# index.html → לשורש · audit-fixes.cjs + boot-smoke.cjs → tests/
git add -A
git commit -m "audit fixes v10.0 — batches 1-3"
git push origin main
```

GitHub Pages יתעדכן תוך 1–2 דקות. רענן בדפדפן עם `Ctrl+Shift+R` (הקובץ נשמר במטמון).

### אפשרות ב׳ — עם היסטוריית ה-commit המלאה

```bash
cd Group-8-War-Room
git am < v10-audit-fixes.patch
git push origin main
```

### אחרי ההעלאה — ודא שעבד

1. פתח את הפלטפורמה, ובפינה צריך להופיע **v10.0 · audit batches 1-3**.
2. לך לסימולטור → הוסף פעולת ייצור שבבים באירופה → **הוסף גם פעולת ייצור מחשבים באירופה**. עד היום זה היה חסום. עכשיו שתיהן אמורות להתקיים.
3. לך למסך הייצוא ברבעון 5 — צריכה להופיע שורת **H6** אדומה עם 30,000 יחידות.

---

## מה נכנס במנה 3

| # | תיקון | חומרה |
|---|---|---|
| **74** | **אזור מריץ יותר משורת ייצור אחת** | **P0** |
| **47** | **מגוון Standard/Deluxe כשני מאגרי ביקוש** | **P1** |
| 6 | טופס A4 — העברת שבבים למפעלי המחשבים | P1 |

### #74 — איך זה נפתר בלי לשבור שום תרחיש שמור

לא שיניתי את הסכמה השמורה. אובייקט האזור נשאר **שורה 0**, ו-`rl.lines[]` מחזיק את השאר. `linesOf()` הוא המנרמל היחיד שכל צרכן עובר דרכו — כך אי-אפשר להוסיף שורה ולשכוח אותה במקום אחד.

עודכנו לעבוד לפי שורות: בדיקת כיסוי שבבים, בדיקת קיבולת, מכירות פנטום, ולידציה, הקרנת תזרים, רו״ה מוקרן, כיסוי החוזה, גיליון ההזנה, והפרסר של ה-AI.

**בסימולטור:** חסימת "פעולה כפולה" הייתה מה שאכף מוצר-אחד-לאזור. עכשיו היא חוסמת רק טופס זהה — A2-3 ו-A2-4 באירופה חיים זה לצד זה.

**שיפור נוסף שנפל מזה:** בדיקת הקיבולת נבדקה מול **מפעל אחד** בלבד. עכשיו היא נבדקת מול הקיבולת כפול מספר המפעלים שהוצהרו למוצר. 48,000 שבבים על 2 מפעלי EU (70,000) הם חוקיים — קודם זה היה מסומן כחריגה.

### #47 — מגוון

שני דגמים = **שתי דרגות שונות** של אותו מוצר באזור. אותה דרגה פעמיים היא דגם אחד (מדריך 4.3) — והבדיקה נועלת את זה.

מקדם ביקוש כולל: דגם אחד ×1.00 · שניים ×1.55 · שלושה ×1.70, עם קניבליזציה של ~35% מהדגם הנמוך לגבוה. **המקדמים הם אומדנים ומסומנים ככאלה** — ובכוונה נמוכים מההנחה בתוכנית שלכם (שהניחה קפיצה מ-15,000 ל-26,000, כלומר ×1.73).

---

## מצב מצטבר: 19 מתוך 96 תיקונים

**כל ארבעת ה-P0 סגורים.** נותרו P1/P2: קשיי חדירה ב-Q4 (#48), היקף ייצור אופטימלי (#59), המלצה כמותית למשרדים (#49), שיפור שיטות (#58), A/P על ייצור (#28), דמי אחסנה (#29), תשואת אג״ח (#35), הכנסת רישוי (#20), אמורטיזציה (#26), לוג תחזית-מול-בפועל (#87).

## אימות

| בדיקה | תוצאה |
|---|---|
| `node tests/audit-fixes.cjs` | **26/26** |
| `node tests/boot-smoke.cjs` (jsdom, boot אמיתי + 9 מסכים) | **אפס שגיאות ריצה** |
| בדיקת תחביר | תקין |

**עדיין לא הורץ:** `./tests/run.sh` הקיים — דורש Chromium שאין כאן. נגעתי ב-`buildInputRows`, `projectCashflow`, `chipGuard`, `capacityCheck`, `contractPlan`, `projectedTechAt` ובסימולטור. **הריצו אותו לפני שאתם מסתמכים על זה בשיעור.**

## שתי פעולות אנושיות שעדיין פתוחות

1. **אימות קיבולת X-EU 35,000** מול הדף המודפס — ה-Datalog מסמן את התא כמשוחזר מ-OCR, וכל היתכנות Q5 תלויה בו.
2. **העלאת דוחות Q1–Q3 האמיתיים** — הם לא היו זמינים, ולכן שום מספר לא אומת מול תוצאות אמיתיות.
